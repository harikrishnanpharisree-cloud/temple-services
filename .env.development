# Loaded automatically by `npm run dev` (Vite mode = development).
# Safe to commit — these point at your own local Supabase stack, not a
# real project, so there's nothing secret here.
#
# This is the PUBLISHABLE key (browser-safe, RLS-restricted) from
# `npx supabase status` — never put the secret key here or in any
# VITE_-prefixed variable; Vite ships those straight into the client bundle.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
